name: Demo
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo Hello GH Actions!
      - name: Print more
        run: |
          echo First line!
          echo Second line!
    
